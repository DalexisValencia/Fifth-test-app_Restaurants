import 'package:fith_app__restaurant/interfaces/CategorySuggested.dart';
import 'package:flutter/material.dart';

const defaultPadding = 0.07;
const double defaultRadius = 15;
const primaryColor = Color(0xFF7F868A); // Gray
const defaultShadow =
    BoxShadow(blurRadius: 6, color: primaryColor, offset: Offset(0, 0));
